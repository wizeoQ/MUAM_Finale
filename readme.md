
# Финальное задание МУАМ

*Выполнил студент Терентьев А.М.*

## Ход выполнения работы

### Шаг 0
Работа сделана на виртуальной машине **Ubuntu 22.04**

### Шаг 1
**"Реализовать генератор чисел"**
Генератор чисел находится в файле Variant_generator.py
с помощью библиотеки random.

> Примечание: В генераторе чисел вариант переопределяется и
> при компиляции всегда один. Для работоспособности программы
> в первоначальном режиме необходимо закомментировать блок:

     Я люблю демократию.
    a = 2
    b = 0
    c = 1

### Шаг 2
Для определения типа данных датасета необходимо вбить
команду `ros2 bag info имя_датасета`

### Шаг 3, 4
Внутри Dockerfile в дир. watchpoints находится установка
среды Ubuntu 22.04, Python, numpy, ROS2, colcon
а также image_viewer и rviz2 для визуализации секвенции изображений
и облака точек.

### Шаг 5
Залить на гитлаб (гитхаб).
2 ветки:
1 - main - добавить генератор и readme
2 - docker - создать докерфайл и добавить rviz2 и image_viewer
слить в одну ветку

## Инструкция для активации
В терминале bash на хосте введите:

Зайдите в директорию watchpoints

    docker build -t ros2-wp .

затем

    xhost +local:docker

потом

    docker run -it --rm \
      --env ROS_DOMAIN_ID=0 \
      --network host \
      -e DISPLAY=$DISPLAY \
      -v /tmp/.X11-unix:/tmp/.X11-unix \
      -v Путь_к_Вашему_датасету:/datasets \
      ros2-wp

Запустится папка с датасетами - помимо обычных ROS2 утилит,
также есть возможность использовать image_viewer и rviz2.

---
